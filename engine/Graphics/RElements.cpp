#include <RElements.h>
#include <triangle.h>
#include <RColour.h>
#include <glm/glm.hpp>

namespace Graphics
{
	namespace RElements 
	{
		
		void drawElement(int id, int primitive , glm::vec3 position, glm::vec2 colour, glm::vec3 rotation)
		{
			switch (primitive) {
				1:
				// get from primitives
				2:
      
				break;
			}

			// get which element you want to draw from primitives
			// assign id
			// get colour from RColour.cpp // its enum so it should be easy 
			// get position
			// get rotation
			// send to buffer function
		};
 e
		void removeElement()
		{
			// which element by id 
			
			// remove it 
		};


		// ask teacher for the namespace tips 
		
	}
}
