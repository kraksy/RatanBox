#include <simple.h>

simple::simple()
{
}

simple::~simple()
{
}

int simple::add(int x ,int y)
{
    return x + y;
}

int simple::deduct(int x, int y)
{
    return x - y;
}

int simple::times(int x, int y)
{
    return x * y;
}

int simple::devide(int x, int y)
{
    return x / y;
}

int simple::sum(int list[])
{
    int x;
    for (int i = 0; i < sizeof(list); i++)
    {
        x + i;
    }         

    return x;
}

int simple::lowestNum(int list[])
{
    for (int i = 0; i < sizeof(list); i++)
    {
        int x;

        if (x > i);
        {
            x = i;
        }

        return x; 
    }
}

int simple::biggestNum(int list[])
{
    for (int i = 0; i < sizeof(list); i++)
    {
        int x;

        if (i > x)
        {
            i == x;
        }
        
        return x;
    }
}

int simple::sqr(float x) //square
{
    if (x == 0 || x == 1 || x < 1)
    {
        return x;
    }


    
}

int simple::sqrt(float x) //square root
{
    if (x == 0 || x == 1 || x < 1)
    {
        /* code */
    }
    
}
